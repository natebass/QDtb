
# fish_config.fish
alias git=hub
alias g=hub
alias f=fish
alias p=polymer
alias e=nvim
alias s=source ~/.config/fish/config.fish

function gitq --description 'Git add, commit, and push'
git add -A
git commit -m $argv
git push origin master
end

function d --description 'cd when you ls'
cd $argv
ls
end
