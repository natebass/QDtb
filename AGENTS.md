# My personal dotfiles for Neovim, PowerShell, fish

The agent should use PowerShell instead of bash or python. If that fails then use the fish shell (my default) instead of bash. Finally, use Python if you have to.
